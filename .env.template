# app/xdevkeys/.env.template — reconstruit en app/xdevkeys/.env par
# docker-entrypoint.sh au démarrage. envconfiguration.inject: true exige
# que ce fichier existe physiquement (ManifestError sinon). Doit résoudre
# à la MÊME valeur que conf/.env.template — une seule variable
# d'environnement DEVKEYS_MASTER_KEY côté plateforme, référencée deux fois.

DEVKEYS_MASTER_KEY=${DEVKEYS_MASTER_KEY}
